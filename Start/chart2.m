values = [5, 10, 15];  % Numeric array for the values
labels = {"Abdlla", "Yunus", "Ayyub"};  % Cell array for the labels

pie(values);       % Create the pie chart with the values
legend(labels);    % Add a legend to label the slices
pause;             % Pause to view the plot
